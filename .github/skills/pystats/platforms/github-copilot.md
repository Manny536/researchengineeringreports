# Platform adapter – GitHub Copilot (pystats)

Install: `<repo>/.github/skills/pystats/` or `~/.copilot/skills/pystats/`  
Source: `agreeya-skills/skills/pystats/`

Code execution for `numpy` / `scipy` / `statsmodels` is conditional on surface tools. Without tools, use transparent hand formulas for small n and mark limits.
